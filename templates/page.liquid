    <main id="page">	     
	    <section class="content">
    	    <div class="wrap">
        	    <header>
                  {% if page.title == "Privacy Policy" %}
                  <h1 style="font-weight: 600;">{{ page.title }}</h1>
                  {% else %}
                    <h1>{{ page.title }}</h1>
                  {% endif %} 
        	    </header>
        	    <div class="page">
        	        {{ page.content }}
        	    </div>
    	    </div>
	    </section>
    </main>